import type { Handle } from "~/lib/handle";

export const handle = {
  breadcrumb: "Instance Library",
} satisfies Handle;

export default function InstanceLibrary() {
  return <p>list instances here sometime idk im lazy</p>;
}
