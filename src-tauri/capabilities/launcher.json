{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "launcher",
  "description": "capability for the launcher window",
  "windows": ["launcher"],
  "permissions": ["core:window:allow-maximize", "core:window:allow-toggle-maximize"]
}
