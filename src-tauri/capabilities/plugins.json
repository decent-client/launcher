{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "plugins",
  "description": "capability for plugins",
  "windows": ["launcher"],
  "permissions": ["window-state:default", "account:default", "instance:default"]
}
